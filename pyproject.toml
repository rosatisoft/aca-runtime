[project]
name = "aca-runtime"
version = "0.1.0"
description = "ACA Runtime for geometry-based criterion supervision."
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "openai",
    "python-dotenv",
    "streamlit",
]

[tool.pytest.ini_options]
pythonpath = ["."]